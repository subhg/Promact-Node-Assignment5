<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%=title %> Page</h1>
    
    <% if (users && users.length >0 ){ %>
     <ul>
        <% users.forEach(user => {%>
        <li><%= user.username %></li>
    <% }); %>
     </ul>
     <% } else { %>
    <p> <%= message %></p>
   <% } %>
</body>
</html>